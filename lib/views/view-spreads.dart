import 'package:flutter/cupertino.dart';

/// 스프레드 뷰 페이지 
class ViewSpreads extends StatelessWidget {
  const ViewSpreads({Key? key}): super(key:key);

  @override
  Widget build(BuildContext context) {
    return const Text("ViewSpreads");
  }
}
